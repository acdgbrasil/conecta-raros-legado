meta {
  name: Forgot Password
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/forgot-password
  body: json
  auth: none
}

body:json {
  {
    "email": "{{email}}"
  }
}

assert {
  res.status: eq 200
}
