meta {
  name: List Permissions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/permissions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

assert {
  res.status: eq 200
  res.body: isArray
  res.body[0].slug: isString
}
