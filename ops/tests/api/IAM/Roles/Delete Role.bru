meta {
  name: Delete Role
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/roles/{{created_role_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

assert {
  res.status: eq 200
}
