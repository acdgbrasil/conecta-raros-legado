meta {
  name: List Roles
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/roles
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

assert {
  res.status: eq 200
  res.body: isArray
  res.body[0].name: isString
}
