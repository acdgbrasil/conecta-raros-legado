meta {
  name: Get Profile (Me)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/users/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

assert {
  res.status: eq 200
  res.body.email: eq admin@conecta.com
  res.body.role: eq ADMIN
}
