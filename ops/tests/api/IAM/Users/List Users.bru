meta {
  name: List Users
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/users?page=1&limit=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

assert {
  res.status: eq 200
  res.body.data: isArray
  res.body.meta.total: isNumber
}
