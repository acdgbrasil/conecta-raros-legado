meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/users/{{created_user_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {
    "name": "Bruno Updated"
  }
}

assert {
  res.status: eq 200
  res.body.name: eq Bruno Updated
}
